#include <stdio.h>

void main(){
    /* int len = 20, width = 15;
    printf("The area of a rectangle with length %d and width %d is %d\n\n", len, width, len*width);


    char myText[] = "Hello world!";
    printf("%s\n", myText);
    */

    int maxScore = 123;
    float score = 91;

   printf("Max score is %d, your score is %d your percentage is %.2f", maxScore, score, score / maxScore);
}
